---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootssssssssssssssss                  # Ingress name
  labels:
    app: springbootssssssssssssssss
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootssssssssssssssss.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootssssssssssssssss               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
